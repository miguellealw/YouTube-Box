# =============================================================================
# Backend Environment Variables (Development) - EXAMPLE TEMPLATE
# =============================================================================
# This file is a template for setting up your local Flask development environment.
#
# SETUP INSTRUCTIONS:
# 1. Copy this file to .env:
#    cp .env.example .env
# 2. Fill in your actual values (DO NOT commit .env to git!)
# =============================================================================

# -----------------------------------------------------------------------------
# API Configuration
# -----------------------------------------------------------------------------
# Frontend server URL (use 'client' service name for Docker)
# For local dev: http://localhost:3000
FRONTEND_SERVER=http://client:3000

# API version prefix
API_VERSION=v1.0

# Server external URL for OAuth callbacks (what browsers see)
# For Docker with nginx: localhost:8080
# For local dev: localhost:5000
SERVER_NAME=server:5000

# URL scheme (http for dev, https for production)
PREFERRED_URL_SCHEME=http

# -----------------------------------------------------------------------------
# Session Management
# -----------------------------------------------------------------------------
# Secret key for Flask sessions
# Generate with: python -c "import os; print(repr(os.urandom(24)))"
SECRET_KEY=generate_secret_key_with_python_urandom

# Session cookie name (customize to avoid conflicts)
SESSION_COOKIE_NAME=youtubebox_session

# -----------------------------------------------------------------------------
# Flask Configuration
# -----------------------------------------------------------------------------
# Flask application entry point
FLASK_APP=youtubebox.py

# Flask environment: development, production, or testing
FLASK_ENV=development

# -----------------------------------------------------------------------------
# Database Configuration
# -----------------------------------------------------------------------------
# PostgreSQL connection string for development
# Format: postgresql://username:password@host:port/database
# For Docker: use 'db' as host
# For local: use 'localhost' as host
DATABASE_URI=postgresql://postgres:your_password@db:5432/YG_DB

# PostgreSQL connection string for testing
TEST_DATABASE_URI=postgresql://postgres:your_password@db:5432/YG_DB_TEST

# -----------------------------------------------------------------------------
# YouTube API Configuration
# -----------------------------------------------------------------------------
# YouTube Data API v3 credentials
# Get these from: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_CLIENT_ID=your_youtube_client_id.apps.googleusercontent.com
YOUTUBE_API_CLIENT_SECRET=your_youtube_client_secret_here

# Client secret JSON filename (should match your downloaded file)
CLIENT_SECRET_FILENAME=client_secret_your_client_id.apps.googleusercontent.com.json

# Allow OAuth over HTTP (ONLY for development - remove in production!)
OAUTHLIB_INSECURE_TRANSPORT=1

# -----------------------------------------------------------------------------
# Auth0 Configuration
# -----------------------------------------------------------------------------
# Auth0 domain (same as frontend)
# Format: your-tenant.us.auth0.com
AUTH0_DOMAIN=your-tenant.us.auth0.com

# JWT signature algorithm (RS256 for Auth0)
ALGORITHMS=["RS256"]

# Auth0 API Audience (API Identifier) - must match frontend
API_AUDIENCE=https://youtubebox/api
